<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFB2C5"
    android:padding="20dp"
    tools:context=".MainActivity">

    <!-- Judul -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📖 Data Santri"
        android:textColor="#2E7D32"      android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:layout_marginTop="20dp" />

    <!-- Tombol Tambah -->
    <Button
        android:id="@+id/btnAddSantri"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tambah Santri ✨"
        android:backgroundTint="#5AB074"  android:textColor="#FFFFFF"        android:textStyle="bold"
        app:cornerRadius="20dp"           android:layout_margin="16dp" />

    <!-- ListView -->
    <ListView
        android:id="@+id/listViewSantri"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="#D8BFD8"
        android:dividerHeight="1dp"
        android:background="#FFFFFF"
        android:padding="8dp"/>
</LinearLayout>
